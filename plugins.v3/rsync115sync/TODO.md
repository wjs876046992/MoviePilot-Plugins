# 115 网盘同步助手 — TODO

> 使用说明见 [USAGE.md](./USAGE.md)，开发进度与问题清单见 [DEVELOPMENT.md](./DEVELOPMENT.md)。
>
> 优先级：**P0** 必须尽快处理 / **P1** 下个迭代 / **P2** 有空再做 / **P3** 观察项

---

## P0 — 必须尽快处理

### [ ] 0. 真机验证：入库发现层重构（2026-09-25 完成，**尚未实机验证**）

代码、单测、门禁、dist 均已完成（见 DEVELOPMENT §4.0f），但以下各项**只有在真机上
才能确认** —— 它们全部依赖真实宿主、真实源目录与真实的 115 挂载。

**发现层（主通道）**

- [ ] 重载插件后看板不报「源端扫描已 X 未推进」（说明 service 真的注册上了）
- [ ] 看板 `/status` 返回的 `source_cursor` 里有全部映射名、且时间在推进
- [ ] 日志出现首轮 `首次源端扫描：基线取当日 0 点`，且有 `🔍 源端扫描：N 个新文件`
- [ ] **手动**往媒体库拷一个文件 → 30 分钟内出现在冷却队列（这是取代事件订阅的核心断言）
- [ ] 往 9KG 目录拷一个文件 → 既被扫描发现，也被 webhook 通知（两条都通、且不重复入队）
- [ ] 重载插件期间拷入的文件 → 下一轮扫描补回（**事件方案做不到这一点**）
- [ ] 确认 `sync_to_115.sh` 与插件**不要同时运行**（同一个源目录、同一个目标，会撞车）

**冷却与上传**

- [ ] 冷却基准符合预期：看板队列里的「基准时刻」对刚入库文件≈其 mtime
- [ ] 一个 5 小时前入库的文件（重载期间错过的）在下一轮同步就被上传，而不是再等 4 小时
- [ ] `delay_hours` 从 2 改到 4 后，**老配置里手工设过的值没有被迁移掉**

**闸门（本次扩容了默认白名单）**

- [ ] 入库一个 `.mp3`/`.flac` → 进队列（此前是 100% 被丢弃）
- [ ] 入库一个 `.sup` 字幕 → 进队列
- [ ] 入库 `.nfo`/`.jpg` → 仍在看板「因扩展名被跳过」提示里按扩展名分桶列出
- [ ] 从旧版本升级的用户：确认扩展名白名单**被迁移到新默认值**（若从未改过）

**调度**

- [ ] 补传队列非空时，冷却到期的文件仍会在下一轮 cron 被处理（不再被补传饿死）

**回归（本次删除面较大）**

- [ ] 插件加载无异常；`/status` 看板完整渲染（新字段都有值、无 undefined）
- [ ] `/rsync_status` 指令回复里出现「源端扫描」「因扩展名被跳过」两行
- [ ] 配置页「源端扫描入库」「源端扫描 Cron 规则」显示与保存正确
- [ ] **老配置兼容**：若你此前把扫描间隔调成过非默认值（如 1 小时），
      升级后日志应出现「已从 N 秒换算为 cron 表达式」而不是静默回到默认值
- [ ] 看板新增的第二行 4 个数字都有值（不是 undefined）、且布局不挤压第一行
- [ ] 点「立即运行一次」→ 返回消息如实说明排队情况（在冷却 N 个 / 最快多久 / 已就绪 N 个）
- [ ] 点「立即运行一次」后：**冷却中的文件没有被提前上传**（这是本按钮的边界）
- [ ] **把浏览器窗口拖窄**：「源端扫描 Cron」输入框应整块换到下一行、完整可见
      （而不是被文字挤成一条缝或挤出可视区）
- [ ] 「观察宽限期（分钟）」保存后重新打开仍是填的值（默认 5）
- [ ] 若你此前改过观察宽限期：启动日志应出现「strm_grace_hours 已废弃」提示，
      且值回落到 5 分钟（**不是**把 6 小时当成 6 分钟）
- [ ] 「启用入库监听」总开关关闭后，**两条来源都停止入队**（扫描与 webhook）
- [ ] strm 交叉验证整条链路不受影响（本次未动它，但它是同一次发布）

---

### [ ] 1. 在真实 MoviePilot 宿主中完整验证 v0.0.4 ~ v0.1.8

**背景**：本插件自 v0.0.4 起的全部改动**从未在真实宿主运行过**。验证仅限静态
检查、隔离逻辑测试与构建产物核对。

**验收清单**：

- [ ] 插件能正常加载，`DEBUG=true` 下无旧导入警告
- [ ] 配置页正常渲染，新增的「上传限流与风控退避」模块与说明文案显示正确
- [ ] 看板正常渲染，补传/退避提示条按预期出现
- [ ] 看板三个按钮（立即同步 / 重试 / 批量同步）在无任务时**可点击**
- [ ] 弹窗标题与内容区间距正常（v0.0.5 修复项）
- [ ] 保存配置后重载，限流参数与补传队列正确恢复
- [ ] 触发一次真实同步，确认 `--files-from` 生效、未全盘扫描

**注意**：验证环境需具备 CD2 挂载与 rsync，否则只能验证 UI 部分。

### [ ] 2. 小规模试跑补传，确认限流行为符合预期

**背景**：限流阈值（200/批、500/窗口、1800 秒）是按经验设定，未经过真实 115
风控考验。

**建议做法**：

- [ ] 先构造小规模候选（如仅补字幕，几十个文件）
- [ ] 观察是否按批次上限与窗口配额推进、进度是否正确持久化
- [ ] 确认看板提示与实际行为一致
- [ ] 根据观察结果回头调整默认阈值

---

## P0.5 — 已实施：Webhook 入库事件（**代码已完成并移除过剩部分**）

> ✅ **代码已实施、已提交**。变更说明暂存 DEVELOPMENT.md 9.13，
> 真机验证清单见 9.12，实施记录与设计差异见 9.10，**两层成因复盘见 9.16/9.17**。
> 实现见 `webhook.py`（纯解析层）+ `__init__.py` 的 webhook 段落，
> 测试见 `tests/v3/rsync115sync/test_webhook_ingest.py` 与
> `test_all_ext_ingest.py`。
>
> ⚠️ **与下方原始设计稿的最大差异：只有一条通道。**
> 设计稿的「通道 B：插件自建 `allow_anonymous` 端点」已于 2026-09-22
> **整体移除**（含四条防护配置与看板字段）。
> 原因：宿主端点对**任何**发送端都收得下报文，只是不认识发送端身份；
> 插件用 `webhook_parser` 认领（发送端带 `source=rsync115sync`）就能拿到它 ——
> 「宿主不替我们解析」不等于「宿主不替我们收」。
> 拆除的完整理由、代价与哨兵见 **DEVELOPMENT §9.18**。
> 下文设计稿保留原样，供对照；**凡涉及通道 B 的段落均已作废**。

### 已核实的关键结论（最终态）

| 来源 | 宿主支持 | 结论 |
|---|---|---|
| **Emby** | ✅ 原生（`app/modules/emby/` 有 `webhook_parser`） | 走平台链路即可 |
| **MDC-ng** | ❌ 宿主**不认识**（40+ 模块中无 mdc-ng/mdcz），但**收得下**其报文 | 同样打平台地址，`source` 填 `rsync115sync`，由插件认领 |
| **自建脚本** | — | 同上；只发 query string 也行（宿主另注册了 `GET /`） |

插件侧必须做的一件事：在 `get_module()` 里声明 `{"webhook_parser": self.webhook_parser}` ——
**只写方法定义等于死代码**（宿主不调用、不报错、无日志），见 DEVELOPMENT §9.17。

### 原始设计稿在哪

原先整段贴在这里（双通道方案、通道 B 的四道防护、实施顺序、待用户提供的四项）。
**已删除** —— 它描述的是「插件自建匿名端点 + 四条防护」这条最终被拆掉的路，
而其中的宿主调研结论（链路形态、来源支持情况、`source` 语义、双通道取舍）
已完整保存在 **DEVELOPMENT.md 第 9 章**，与本文件重复只会让两处逐渐失真。
查找入口：

| 想查什么 | 去哪 |
|---|---|
| 宿主 webhook 链路的原始调研结论 | DEVELOPMENT §9.2 / §9.3 |
| 双通道方案与当时的安全推理（历史） | DEVELOPMENT §9.6 / §9.7 |
| **为什么最终砍成单通道** | DEVELOPMENT §9.18 |
| 真机踩到的两层成因 | DEVELOPMENT §9.16 / §9.17 |

## P0.7 — 待实机确认：补生成 strm 的真实闭环

> v0.1.9 引入「先尝试生成 strm」。**两次设计决策已被实测推翻过**
> （见 DEVELOPMENT.md 3.12），因此这条链路同样**必须实机验证**，不能只看单测。

- [ ] 在配置页为目标映射填好「网盘目录」，且确认该路径已配在 P115StrmHelper 的
      「全量同步路径」里
- [ ] 看板点「先尝试生成 strm」，确认：命令真的送达（助手日志出现
      `【全量STRM生成】网盘媒体目录 ID 获取成功: <网盘目录>`）、助手**生成了文件**
      （日志 `总共生成 N 个 STRM 文件`，N 不为 0）、.strm 真的出现在本地 strm 目录
- [ ] 确认这些文件**移回「观察中」**并显示「已请求生成」标记（按 1h 固定窗口计时，
      不是宽限期配置值）
- [ ] 确认观察期条目的「检查 strm」按钮能即时判定（已生成 ⇒ 立即解除；未到期 ⇒ 继续等待）
- [ ] 确认窗口到期仍无 strm 时，条目回到疑似清单并带「补生成后仍无」标记
- [ ] 确认**移动发生在命令确实发出之后**：把助手的「全量同步路径」临时改错，
      再点补生成 —— 预检应直接拦下（不发命令、条目不动）；若预检因故失效而命令
      真被拒收，条目也必须留在疑似清单
- [ ] 确认补生成条目回到观察期后，执行一次「扫描缺 strm 的文件」**不会**把它
      打回疑似（应体现在扫描结果的「已在观察期未重复计入」计数里）
- [ ] 若助手日志报「路径匹配错误」→ 说明第 1 步的助手配置没对上，需记录并补充提示文案

**云端可见性探测 —— 已废弃，手工确认清单随之作废**

- [x] ~~实机确认云端可见性结论的三种显示~~
      → **v0.3.0 整体删除**：该观测点对改名失败这个主成因没有鉴别力
      （CD2 挂载视图必然显示成「可见、大小一致」，见 DEVELOPMENT 3.11 末尾）。
      清单里这几条手工验证项已随功能一起作废 —— **不需要再验**。
- [ ] 新的判据只有一条：`.strm` 存不存在。若要实机确认，验这两条即可：
  - [ ] 删掉某个已同步文件的 `.strm`，看它到期后是否进入疑似清单
  - [ ] 重新生成该 `.strm`，看条目是否自动解除（不必手工干预）

### [ ] 14. 补生成的进度可见性（观察项）

助手是异步长任务，插件侧拿不到完成回调，当前只能靠「观察窗口到期后看结果」。
补生成条目已移回观察期，因此用户至少能点「检查 strm」主动问一次，不必盯着
疑似清单刷新。若仍反馈「点了之后不知道在跑还是没跑」，可考虑：
- [ ] 读取助手日志判断任务是否在跑（耦合其日志格式，**谨慎**）
- [ ] 或仅提供「去助手插件页看进度」的链接

---

## P1 — 下个迭代

### [~] 3. 补充自动化测试（核心链路已完成，外围待补）

**已完成**：`tests/v3/rsync115sync/` 共 267 项，覆盖核心同步链路 ——
入库事件入队判据与映射归属、幂等、路径来源回退、限流三层判定与配额边界、
strm 五态流转、忽略规则语义、主动扫描、补生成目标折算与上限整批拒绝、
补生成后的重新计时与计时基准（双钟）、观察期手动检查、台账列视图
（`LedgerFieldMap` 只碰一列 / pop 置 NULL / 不凭空插入）、以及一组
**结构哨兵**（`test_no_cloud_verdict.py`：用 AST 断言已删的「云端可见性」
判据不得复活 —— 注释里解释"为什么删掉它"是允许的，所以不能靠 grep）。
已用变异测试验证用例真的会失败，不是「永远绿灯」。

**⚠️ 但有一个限制必须知道**：本机没有 `../MoviePilot` 后端，`tests/conftest.py`
跑不起来，因此这 117 项是在**自建桩宿主**下执行的。真实走 `app.plugins.rsync115sync`
生产导入路径，但与真实宿主仍有差距。**不要因为有了测试就认为核心链路已安全**。

**仍待补**（按价值排序）：

- [ ] `_execute_sync` 端到端（仍需拆分/桩 rsync）；批次结算纯函数已由 `test_batch_settlement.py` 覆盖（P0-1/P0-2）
- [x] ~~force 绕过配额预扣/批次上限~~ → v0.2.1（P0-2）
- [x] ~~exit 23/24 时对账通过文件不出队/不 arm strm~~ → v0.2.1（P0-1）
- [x] ~~补齐清单出账早于批次上限截断（被截断条目永久丢失）~~ → v0.2.1 review 修复
- [x] ~~队列 key 反向解析 split(":", 1) 切错（任务名含冒号）~~ → v0.2.1 review 修复，收口 `paths.rel_path_of_key`
- [x] ~~版本字段漂移的**门禁盲区**~~ → alpha（`check_plugin_versions.py` 现比对插件自带
      `package.json` 与 `Config.vue` 版本 chip；**默认只警告不阻断**，见其说明 ——
      首次启用时仓库里已有 5 个插件带历史漂移，一律判失败只会被放宽）
- [x] ~~`_build_backfill_candidates` 候选口径~~ → alpha `test_backfill_and_parsing.py`
- [x] ~~`_delete_dest_files_for_retry` 删除护栏（破坏性操作）~~ → alpha `test_delete_guardrails.py`（9 条；原有两条「顺带清残留」用例随 v0.3.0 删除该能力一并移除）
- [x] ~~`_migrate_legacy_defaults` 自定义值保护~~ → alpha（含「改过一个字符就不动」的参数化用例）
- [x] ~~`_parse_confirm_indices` 序号解析~~ → alpha（含越界**丢弃而非夹边界**的取向）
- [x] ~~`_TOLERATED_EXIT_CODES` 退出码分类~~ → alpha
- [x] ~~`_find_sidecar_files` 伴生字幕匹配~~ → alpha（含跨集误纳、目录冒充字幕两条边界）

### [ ] 4. 修复 fork 推送不触发 Release 工作流

**背景**：本仓库是 fork，push 到 main 不触发 Plugin Release（0.2.0 发布时再次确认：
push d66b905 后 runs 列表无新条目，仍需手动 dispatch），每次发版需手动
`gh workflow run "Plugin Release"`。

**方案**：在仓库 Settings → Actions 中开启 fork 的 push 触发（需仓库管理员权限，
非代码改动）。

### [ ] 5. 清理幽灵文件的能力（需谨慎评估）

**背景**：旧 README 曾宣传 `/rsync_clean`「扫描并清理网盘中残留的 `..*` 临时
垃圾文件」，但**代码从未实现**，v0.0.8 已从文档移除。

**不建议按原设想实现**：扫全盘找 `..*` 并删除，是「遍历 + 破坏性操作」，
恰好命中要规避的风控场景。

**若确实要做，应改为**：

- [ ] 不做全盘扫描，只处理**已知路径**（例如异常清单里出现过的文件所在目录）
- [ ] 默认 dry-run，列出待删清单让用户确认
- [ ] 严格限制单次删除数量并受限流配额约束

### [ ] 6. 补充截图 / 图示到使用文档

- [ ] 配置页目录映射填写示例截图
- [ ] 看板界面说明标注图
- [ ] 补传流程示意图

---

## P2 — 有空再做

### [ ] 6.5 超慢写入：到期时复核 mtime 是否仍等于发现时的快照

**已知残留风险**（DEVELOPMENT §4.0f ④ 记录）：冷却基准取 `min(发现时刻, mtime)`
能覆盖绝大多数场景，但不覆盖「某工具原地写入、且从第一刻起就带着源端老时间戳」
（既不 rename、也不先写当前时间）。这类文件会在冷却期内仍被写入，到期时照样上传。

**现状**：实测未观察到这种写入形态；真出现也不静默损坏 —— 会落进残留/对账异常
清单（§3.10 / §3.11 为配套护栏），代价是一次无效上传。

**真要根除的做法**：入队时记下 mtime 快照，到期时比对；不一致就顺延一个冷却窗口。
代价是 `_pending_queue` 的**值**从 `float` 变成 `{basis, hash/mtime}`，涉及持久化
结构迁移（需处理旧格式与新格式共存）。

- [ ] 先确认真机上是否真的出现过"原地写入 + 老时间戳"（若没有，不必做）
- [ ] 若要做：设计兼容旧格式的迁移（读旧值时按"mtime=基准"补齐快照）
- [ ] 注意顺延不能无限循环：连续 N 轮仍在变化应告警而不是永远等

### [ ] 6.6 源端扫描的重叠窗口是否可以从 120 秒调大

当前 120 秒是为"同一秒批量落盘"设的。若真机上观察到更多边界漏（例如 NFS 的
mtime 粒度更粗），可调大 —— 代价只是重复扫到已入队的文件（由幂等吸收）。
`SOURCE_CURSOR_OVERLAP_SECS` 一个常量即可调整。

### [ ] 7. 配额失败时不退还（已知取舍）

rsync 失败时配额已扣除且不退还，连续失败会白耗窗口额度。这是**有意的保守设计**
（宁可少传不可多传）。若后续有用户反馈，可考虑「失败批次退还配额」，
但需注意别因此打开绕过限流的口子。

### [ ] 8. 补传候选口径精度

当前候选基于本地推断，**必然包含已同步但从未入过队的文件**（靠 `--size-only`
在传输阶段跳过，每次仍耗一次 stat）。

- [ ] 调研 CD2 是否提供可靠的目标端清单接口，若有可显著降低成本
- [ ] 或考虑维护轻量「已同步台账」（需权衡：台账损坏会导致全库重传，
      这正是当前无状态设计想避免的）

### [ ] 9. 限流状态可视化

- [ ] 看板显示当前窗口已用/总额度、距窗口重置时间
- [ ] 历史退避次数统计，帮助用户判断阈值是否合理

### [ ] 10. `/rsync_status` 输出包含补传与限流信息

当前 `/rsync_status` 主要输出冷却队列与异常清单，补传队列进度与限流状态
信息有限，可考虑补全。

---

## P3 — 观察项

### [ ] 11. 补传排空期间常规同步被推迟

`_scheduled_sync` 中补传优先，同轮只跑一种模式（共用执行锁与窗口配额）。
补传队列未排空时常规同步会推迟。队列自终止，暂可接受，**若用户反馈延迟明显**
再考虑改为按比例分配配额。

### [ ] 12. 大文件与超时参数的匹配

I/O 超时已对齐 shell 的 600 秒，但 `task_timeout` 默认 3600 秒（单次任务总上限）
是否够用，取决于媒体库规模与网络状况，需实机观察。

### [ ] 13. 多映射对时的配额分配

当前所有映射对共享同一份窗口配额，**先跑的映射对会先消耗额度**。
若用户有多组映射且希望公平分配，需重新设计（暂无明显需求）。

---

## 已完成（归档）

- [x] ~~看板按钮永久禁用~~ → v0.0.4
- [x] ~~弹窗标题与内容间距过近~~ → v0.0.5
- [x] ~~rsync 非零退出码误判为成功~~ → v0.0.6
- [x] ~~缺少上传限流与风控退避~~ → v0.0.7
- [x] ~~缺少存量补传；媒体与字幕未成组~~ → v0.0.8
- [x] ~~force 全量扫描可无限触发；缺 include 过滤~~ → v0.0.9
- [x] ~~默认扩展名缺字幕；超时偏短；缺 `..*` 排除~~ → v0.0.10
- [x] ~~注释不足；配置页说明不清晰~~ → v0.0.11
- [x] ~~README 与代码不符（`--inplace` 宣称、`/rsync_clean` 幻影命令）~~ → v0.0.8
