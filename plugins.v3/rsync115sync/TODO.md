# 115 网盘同步助手 — TODO

> 使用说明见 [USAGE.md](./USAGE.md)，开发进度与问题清单见 [DEVELOPMENT.md](./DEVELOPMENT.md)。
>
> 优先级：**P0** 必须尽快处理 / **P1** 下个迭代 / **P2** 有空再做 / **P3** 观察项

---

## P0 — 必须尽快处理

### [ ] 1. 在真实 MoviePilot 宿主中完整验证 v0.0.4 ~ v0.1.8

**背景**：本插件自 v0.0.4 起的全部改动**从未在真实宿主运行过**。验证仅限静态
检查、隔离逻辑测试与构建产物核对。

**验收清单**：

- [ ] 插件能正常加载，`DEBUG=true` 下无旧导入警告
- [ ] 配置页正常渲染，新增的「上传限流与风控退避」模块与说明文案显示正确
- [ ] 看板正常渲染，补传/退避提示条按预期出现
- [ ] 看板三个按钮（立即同步 / 重试 / 批量同步）在无任务时**可点击**
- [ ] 弹窗标题与内容区间距正常（v0.0.5 修复项）
- [ ] 保存配置后重载，限流参数与补传队列正确恢复
- [ ] 触发一次真实同步，确认 `--files-from` 生效、未全盘扫描

**注意**：验证环境需具备 CD2 挂载与 rsync，否则只能验证 UI 部分。

### [ ] 2. 小规模试跑补传，确认限流行为符合预期

**背景**：限流阈值（200/批、500/窗口、1800 秒）是按经验设定，未经过真实 115
风控考验。

**建议做法**：

- [ ] 先构造小规模候选（如仅补字幕，几十个文件）
- [ ] 观察是否按批次上限与窗口配额推进、进度是否正确持久化
- [ ] 确认看板提示与实际行为一致
- [ ] 根据观察结果回头调整默认阈值

---

## P0.5 — 已实施（alpha，未发布版本号）：Webhook 入库事件

> ✅ **代码已实施、已提交；尚未发布**（alpha 阶段按仓库策略不占版本号，
> 三处版本字段保持 `0.1.10`，变更说明暂存 DEVELOPMENT.md 9.13，真机验证清单见 9.12）。
> 实现见 `webhook.py` + `__init__.py` 的 webhook 段落，
> 测试 62 项（`tests/v3/rsync115sync/test_webhook_ingest.py`），
> 实施记录与设计差异见 DEVELOPMENT.md 9.10。
>
> 下面保留原始设计稿，供对照「设计 vs 实现」；已确认的差异点标在 §9.10。

### 原设计稿（保留对照）


> 状态：**仅设计，未写代码**。方案依据宿主源码调研得出，实施前需用户补充三样东西
> （见文末）。调研结论见 DEVELOPMENT.md 第 9 章。

### 目标

为「新文件入库」增加**第二来源**，覆盖现有 `TransferComplete` 事件监听不到的场景
（手动放入、外部工具搬入、整理事件漏发）。来源两处：**Emby** 与 **MDC-ng**。

### 关键调研结论（已核实源码，非推测）

宿主已内建完整 webhook 链路：

```
Emby/MDC ──HTTP──▶ POST /api/v1/webhook/?token=API_TOKEN&source=<实例名>
                        ↓ verify_apitoken（宿主鉴权）
                   WebhookChain.message(body, form, args)
                        ↓ webhook_parser()   ← 由**模块**提供解析，插件不参与
                   eventmanager.send_event(EventType.WebhookMessage, info)
                        ↓
                   插件 @eventmanager.register(EventType.WebhookMessage)
```

| 来源 | 宿主支持 | 结论 |
|---|---|---|
| **Emby** | ✅ 原生（`app/modules/emby/` 有 `webhook_parser`） | 走宿主链路即可，**无需自建端点** |
| **MDC-ng** | ❌ **宿主不认识**（40+ 模块中无 mdc-ng/mdcz） | **必须插件自建 `allow_anonymous` 端点** |

插件自建端点能力已核实：`app/adapters/web/plugin/routes.py:116-125` 支持
`allow_anonymous`（跳过宿主鉴权）与 `auth`（bear/apikey）。

**仓库内已有先例**：`plugins.v3/watchsync` 已实现 `WebhookMessage` 订阅，同时支持
`emby` 与 `zspace`，可作为参照。注意它**不用 `event.snapshot()`**，而是
`getattr(event_data, ...)` 直接读 —— 因为 `WebhookMessage` 未登记在
`_SNAPSHOT_EVENTS`（`app/runtime/event/contracts.py:100` 只登记类型）。

### 方案：双通道

- **通道 A（Emby）**：订阅 `EventType.WebhookMessage`，`channel == "emby"` 时取
  `item_path`，只处理入库类事件（`library.new` 等），播放类忽略。
  Emby 后台 Webhook 填：
  `http://<moviepilot>:3001/api/v1/webhook/?token=<API_TOKEN>&source=<emby实例名>`
- **通道 B（MDC-ng）**：插件自建 `POST /webhook/mdc`，`allow_anonymous: true`。

两条通道最终**合流到同一入口**：

```
webhook → 取路径 → 校验归属映射（复用 paths.pair_for_path）
                 → 校验源端文件存在 → 入冷却队列（复用现有幂等入队）
```

**设计要点**：webhook 是**补充**而非替代。同一文件可能同时从两条路进来，
现有 `_pending_queue` 的重复检测天然幂等（不刷新时间戳），不会重复入队。

### ⚠️ 通道 B 的安全风险（实施时必须解决）

`allow_anonymous: true` 意味着**知道 URL 就能往插件灌数据** —— 可伪造入库、
可灌大量伪造路径撑爆队列。防护优先级：

1. 自定义 Header 密钥（最干净）—— 需 MDC-ng 支持
2. URL 参数密钥 `?token=xxx` —— 次选
3. **路径白名单校验（无条件要做）**：进来的路径必须落在已配置的
   `sync_pairs` 源目录内，与 `paths.pair_for_path()` 同判据，能挡掉绝大多数伪造数据
4. 来源 IP 白名单（兜底）

### 实施顺序

1. **先做通道 A（Emby）** —— 低风险、宿主链路已通、可立即验证
2. **通道 B 需先拿到真实报文**：先写一个**只记日志、不做处理**的临时 debug 端点，
   配上去观察真实 payload，再写解析器。
   **不要凭猜测写解析器** —— 本项目已多次吃过「凭推测写机制」的亏（见 3.8.1）

### 实施前需要用户提供

- [ ] **MDC-ng 的 webhook 报文样例**（一份真实 JSON）—— 决定字段名与事件命名
- [ ] **MDC-ng 是否支持自定义 Header / URL 参数** —— 决定鉴权方案
- [ ] **实际要监听的事件类型**（仅新入库？还是含播放/收藏）
- [ ] 确认 Emby 通道是**补充**还是**替代**现有整理事件监听
      （补充则正好覆盖「手动放入/外部搬入」这类现有链路看不到的场景）

---

## P0.7 — 待实机确认：补生成 strm 的真实闭环

> v0.1.9 引入「先尝试生成 strm」。**两次设计决策已被实测推翻过**
> （见 DEVELOPMENT.md 3.12），因此这条链路同样**必须实机验证**，不能只看单测。

- [ ] 在配置页为目标映射填好「网盘目录」，且确认该路径已配在 P115StrmHelper 的
      「全量同步路径」里
- [ ] 看板点「先尝试生成 strm」，确认：命令真的送达（助手日志出现
      `【全量STRM生成】网盘媒体目录 ID 获取成功: <网盘目录>`）、助手**生成了文件**
      （日志 `总共生成 N 个 STRM 文件`，N 不为 0）、.strm 真的出现在本地 strm 目录
- [ ] 确认这些文件**移回「观察中」**并显示「已请求生成」标记（按 1h 固定窗口计时，
      不是宽限期配置值）
- [ ] 确认观察期条目的「检查 strm」按钮能即时判定（已生成 ⇒ 立即解除；未到期 ⇒ 继续等待）
- [ ] 确认窗口到期仍无 strm 时，条目回到疑似清单并带「补生成后仍无」标记
- [ ] 确认**移动发生在命令确实发出之后**：把助手的「全量同步路径」临时改错，
      再点补生成 —— 预检应直接拦下（不发命令、条目不动）；若预检因故失效而命令
      真被拒收，条目也必须留在疑似清单
- [ ] 确认补生成条目回到观察期后，执行一次「扫描缺 strm 的文件」**不会**把它
      打回疑似（应体现在扫描结果的「已在观察期未重复计入」计数里）
- [ ] 若助手日志报「路径匹配错误」→ 说明第 1 步的助手配置没对上，需记录并补充提示文案

**云端可见性探测**（v0.1.9 新增，标注「纯本地读取」的那条结论）：

- [ ] 确认目标端确实是 CD2 挂载点、容器内可读（探测读的是 `pair.dest` 下的大小）
- [ ] 对一个**云端确实没有**的文件：清单里应显示「云端不可见…删旧重传可修」
- [ ] 对一个**云端存在且大小一致**的文件：应显示「云端可见且大小一致…请先查
      strm 生成侧」，且此时点「删旧重传」应被**拦下并说明理由**
- [ ] 断掉 / 卸载 CD2 挂载后刷新看板：整批应显示「云端可见性未知」，
      **不得**显示成「云端不可见」（后者会误导用户去删一批好文件）
- [ ] 挂载恢复后，「删旧重传」应照常可用（unknown 必须 fail-open）

### [ ] 14. 补生成的进度可见性（观察项）

助手是异步长任务，插件侧拿不到完成回调，当前只能靠「观察窗口到期后看结果」。
补生成条目已移回观察期，因此用户至少能点「检查 strm」主动问一次，不必盯着
疑似清单刷新。若仍反馈「点了之后不知道在跑还是没跑」，可考虑：
- [ ] 读取助手日志判断任务是否在跑（耦合其日志格式，**谨慎**）
- [ ] 或仅提供「去助手插件页看进度」的链接

---

## P1 — 下个迭代

### [~] 3. 补充自动化测试（核心链路已完成，外围待补）

**已完成**：`tests/v3/rsync115sync/` 共 267 项，覆盖核心同步链路 ——
入库事件入队判据与映射归属、幂等、路径来源回退、限流三层判定与配额边界、
strm 五态流转、忽略规则语义、主动扫描、补生成目标折算与上限整批拒绝、
补生成后的重新计时与计时基准（双钟）、观察期手动检查、目标端可见性探测
（含挂载未就绪必须退化为 unknown、探测只写不删、重传前重新探测）。
已用变异测试验证用例真的会失败，不是「永远绿灯」。

**⚠️ 但有一个限制必须知道**：本机没有 `../MoviePilot` 后端，`tests/conftest.py`
跑不起来，因此这 117 项是在**自建桩宿主**下执行的。真实走 `app.plugins.rsync115sync`
生产导入路径，但与真实宿主仍有差距。**不要因为有了测试就认为核心链路已安全**。

**仍待补**（按价值排序）：

- [ ] `_execute_sync` 的批次上限与配额闸门（方法 572 行，需先按阶段拆分才好测）
- [ ] `_build_backfill_candidates` 候选口径（排除队列/异常/忽略项）
- [ ] `_delete_dest_files_for_retry` 删除护栏（**破坏性操作，优先级应提前**）
- [ ] `_migrate_legacy_defaults` 自定义值保护（**重点：自定义值必须不动**）
- [ ] `_parse_confirm_indices` 序号解析（`1`、`1,2`、`1-2 4`、`all`）
- [ ] `_TOLERATED_EXIT_CODES` 退出码分类
- [ ] `_find_sidecar_files` 伴生字幕匹配（含跨集误纳的边界）

### [ ] 4. 修复 fork 推送不触发 Release 工作流

**背景**：本仓库是 fork，push 到 main 不触发 Plugin Release，每次发版需手动
`gh workflow run`。

**方案**：在仓库 Settings → Actions 中开启 fork 的 push 触发（需仓库管理员权限，
非代码改动）。

### [ ] 5. 清理幽灵文件的能力（需谨慎评估）

**背景**：旧 README 曾宣传 `/rsync_clean`「扫描并清理网盘中残留的 `..*` 临时
垃圾文件」，但**代码从未实现**，v0.0.8 已从文档移除。

**不建议按原设想实现**：扫全盘找 `..*` 并删除，是「遍历 + 破坏性操作」，
恰好命中要规避的风控场景。

**若确实要做，应改为**：

- [ ] 不做全盘扫描，只处理**已知路径**（例如异常清单里出现过的文件所在目录）
- [ ] 默认 dry-run，列出待删清单让用户确认
- [ ] 严格限制单次删除数量并受限流配额约束

### [ ] 6. 补充截图 / 图示到使用文档

- [ ] 配置页目录映射填写示例截图
- [ ] 看板界面说明标注图
- [ ] 补传流程示意图

---

## P2 — 有空再做

### [ ] 7. 配额失败时不退还（已知取舍）

rsync 失败时配额已扣除且不退还，连续失败会白耗窗口额度。这是**有意的保守设计**
（宁可少传不可多传）。若后续有用户反馈，可考虑「失败批次退还配额」，
但需注意别因此打开绕过限流的口子。

### [ ] 8. 补传候选口径精度

当前候选基于本地推断，**必然包含已同步但从未入过队的文件**（靠 `--size-only`
在传输阶段跳过，每次仍耗一次 stat）。

- [ ] 调研 CD2 是否提供可靠的目标端清单接口，若有可显著降低成本
- [ ] 或考虑维护轻量「已同步台账」（需权衡：台账损坏会导致全库重传，
      这正是当前无状态设计想避免的）

### [ ] 9. 限流状态可视化

- [ ] 看板显示当前窗口已用/总额度、距窗口重置时间
- [ ] 历史退避次数统计，帮助用户判断阈值是否合理

### [ ] 10. `/rsync_status` 输出包含补传与限流信息

当前 `/rsync_status` 主要输出冷却队列与异常清单，补传队列进度与限流状态
信息有限，可考虑补全。

---

## P3 — 观察项

### [ ] 11. 补传排空期间常规同步被推迟

`_scheduled_sync` 中补传优先，同轮只跑一种模式（共用执行锁与窗口配额）。
补传队列未排空时常规同步会推迟。队列自终止，暂可接受，**若用户反馈延迟明显**
再考虑改为按比例分配配额。

### [ ] 12. 大文件与超时参数的匹配

I/O 超时已对齐 shell 的 600 秒，但 `task_timeout` 默认 3600 秒（单次任务总上限）
是否够用，取决于媒体库规模与网络状况，需实机观察。

### [ ] 13. 多映射对时的配额分配

当前所有映射对共享同一份窗口配额，**先跑的映射对会先消耗额度**。
若用户有多组映射且希望公平分配，需重新设计（暂无明显需求）。

---

## 已完成（归档）

- [x] ~~看板按钮永久禁用~~ → v0.0.4
- [x] ~~弹窗标题与内容间距过近~~ → v0.0.5
- [x] ~~rsync 非零退出码误判为成功~~ → v0.0.6
- [x] ~~缺少上传限流与风控退避~~ → v0.0.7
- [x] ~~缺少存量补传；媒体与字幕未成组~~ → v0.0.8
- [x] ~~force 全量扫描可无限触发；缺 include 过滤~~ → v0.0.9
- [x] ~~默认扩展名缺字幕；超时偏短；缺 `..*` 排除~~ → v0.0.10
- [x] ~~注释不足；配置页说明不清晰~~ → v0.0.11
- [x] ~~README 与代码不符（`--inplace` 宣称、`/rsync_clean` 幻影命令）~~ → v0.0.8
