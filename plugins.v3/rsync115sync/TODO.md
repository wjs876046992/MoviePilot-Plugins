# 115 网盘同步助手 — TODO

> 使用说明见 [USAGE.md](./USAGE.md)，开发进度与问题清单见 [DEVELOPMENT.md](./DEVELOPMENT.md)。
>
> 优先级：**P0** 必须尽快处理 / **P1** 下个迭代 / **P2** 有空再做 / **P3** 观察项

---

## P0 — 必须尽快处理

### [ ] 1. 在真实 MoviePilot 宿主中完整验证 v0.0.4 ~ v0.1.6

**背景**：本插件自 v0.0.4 起的全部改动**从未在真实宿主运行过**。验证仅限静态
检查、隔离逻辑测试与构建产物核对。

**验收清单**：

- [ ] 插件能正常加载，`DEBUG=true` 下无旧导入警告
- [ ] 配置页正常渲染，新增的「上传限流与风控退避」模块与说明文案显示正确
- [ ] 看板正常渲染，补传/退避提示条按预期出现
- [ ] 看板三个按钮（立即同步 / 重试 / 批量同步）在无任务时**可点击**
- [ ] 弹窗标题与内容区间距正常（v0.0.5 修复项）
- [ ] 保存配置后重载，限流参数与补传队列正确恢复
- [ ] 触发一次真实同步，确认 `--files-from` 生效、未全盘扫描

**注意**：验证环境需具备 CD2 挂载与 rsync，否则只能验证 UI 部分。

### [ ] 2. 小规模试跑补传，确认限流行为符合预期

**背景**：限流阈值（200/批、500/窗口、1800 秒）是按经验设定，未经过真实 115
风控考验。

**建议做法**：

- [ ] 先构造小规模候选（如仅补字幕，几十个文件）
- [ ] 观察是否按批次上限与窗口配额推进、进度是否正确持久化
- [ ] 确认看板提示与实际行为一致
- [ ] 根据观察结果回头调整默认阈值

---

## P1 — 下个迭代

### [~] 3. 补充自动化测试（核心链路已完成，外围待补）

**已完成**（alpha `7a60ee3`）：`tests/v3/rsync115sync/` 共 117 项，覆盖核心同步
链路 —— 入库事件入队判据与映射归属、幂等、路径来源回退、限流三层判定与配额边界、
strm 五态流转、忽略规则语义。已用变异测试验证用例真的会失败，不是「永远绿灯」。

**⚠️ 但有一个限制必须知道**：本机没有 `../MoviePilot` 后端，`tests/conftest.py`
跑不起来，因此这 117 项是在**自建桩宿主**下执行的。真实走 `app.plugins.rsync115sync`
生产导入路径，但与真实宿主仍有差距。**不要因为有了测试就认为核心链路已安全**。

**仍待补**（按价值排序）：

- [ ] `_execute_sync` 的批次上限与配额闸门（方法 572 行，需先按阶段拆分才好测）
- [ ] `_build_backfill_candidates` 候选口径（排除队列/异常/忽略项）
- [ ] `_delete_dest_files_for_retry` 删除护栏（**破坏性操作，优先级应提前**）
- [ ] `_migrate_legacy_defaults` 自定义值保护（**重点：自定义值必须不动**）
- [ ] `_parse_confirm_indices` 序号解析（`1`、`1,2`、`1-2 4`、`all`）
- [ ] `_TOLERATED_EXIT_CODES` 退出码分类
- [ ] `_find_sidecar_files` 伴生字幕匹配（含跨集误纳的边界）

### [ ] 4. 修复 fork 推送不触发 Release 工作流

**背景**：本仓库是 fork，push 到 main 不触发 Plugin Release，每次发版需手动
`gh workflow run`。

**方案**：在仓库 Settings → Actions 中开启 fork 的 push 触发（需仓库管理员权限，
非代码改动）。

### [ ] 5. 清理幽灵文件的能力（需谨慎评估）

**背景**：旧 README 曾宣传 `/rsync_clean`「扫描并清理网盘中残留的 `..*` 临时
垃圾文件」，但**代码从未实现**，v0.0.8 已从文档移除。

**不建议按原设想实现**：扫全盘找 `..*` 并删除，是「遍历 + 破坏性操作」，
恰好命中要规避的风控场景。

**若确实要做，应改为**：

- [ ] 不做全盘扫描，只处理**已知路径**（例如异常清单里出现过的文件所在目录）
- [ ] 默认 dry-run，列出待删清单让用户确认
- [ ] 严格限制单次删除数量并受限流配额约束

### [ ] 6. 补充截图 / 图示到使用文档

- [ ] 配置页目录映射填写示例截图
- [ ] 看板界面说明标注图
- [ ] 补传流程示意图

---

## P2 — 有空再做

### [ ] 7. 配额失败时不退还（已知取舍）

rsync 失败时配额已扣除且不退还，连续失败会白耗窗口额度。这是**有意的保守设计**
（宁可少传不可多传）。若后续有用户反馈，可考虑「失败批次退还配额」，
但需注意别因此打开绕过限流的口子。

### [ ] 8. 补传候选口径精度

当前候选基于本地推断，**必然包含已同步但从未入过队的文件**（靠 `--size-only`
在传输阶段跳过，每次仍耗一次 stat）。

- [ ] 调研 CD2 是否提供可靠的目标端清单接口，若有可显著降低成本
- [ ] 或考虑维护轻量「已同步台账」（需权衡：台账损坏会导致全库重传，
      这正是当前无状态设计想避免的）

### [ ] 9. 限流状态可视化

- [ ] 看板显示当前窗口已用/总额度、距窗口重置时间
- [ ] 历史退避次数统计，帮助用户判断阈值是否合理

### [ ] 10. `/rsync_status` 输出包含补传与限流信息

当前 `/rsync_status` 主要输出冷却队列与异常清单，补传队列进度与限流状态
信息有限，可考虑补全。

---

## P3 — 观察项

### [ ] 11. 补传排空期间常规同步被推迟

`_scheduled_sync` 中补传优先，同轮只跑一种模式（共用执行锁与窗口配额）。
补传队列未排空时常规同步会推迟。队列自终止，暂可接受，**若用户反馈延迟明显**
再考虑改为按比例分配配额。

### [ ] 12. 大文件与超时参数的匹配

I/O 超时已对齐 shell 的 600 秒，但 `task_timeout` 默认 3600 秒（单次任务总上限）
是否够用，取决于媒体库规模与网络状况，需实机观察。

### [ ] 13. 多映射对时的配额分配

当前所有映射对共享同一份窗口配额，**先跑的映射对会先消耗额度**。
若用户有多组映射且希望公平分配，需重新设计（暂无明显需求）。

---

## 已完成（归档）

- [x] ~~看板按钮永久禁用~~ → v0.0.4
- [x] ~~弹窗标题与内容间距过近~~ → v0.0.5
- [x] ~~rsync 非零退出码误判为成功~~ → v0.0.6
- [x] ~~缺少上传限流与风控退避~~ → v0.0.7
- [x] ~~缺少存量补传；媒体与字幕未成组~~ → v0.0.8
- [x] ~~force 全量扫描可无限触发；缺 include 过滤~~ → v0.0.9
- [x] ~~默认扩展名缺字幕；超时偏短；缺 `..*` 排除~~ → v0.0.10
- [x] ~~注释不足；配置页说明不清晰~~ → v0.0.11
- [x] ~~README 与代码不符（`--inplace` 宣称、`/rsync_clean` 幻影命令）~~ → v0.0.8
